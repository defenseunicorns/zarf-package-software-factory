resources:
  - base
  - secrets.yaml

configMapGenerator:
  - name: redis-common
    namespace: softwarefactoryaddons
    behavior: replace
    files:
      - values.yaml=redis/values.yaml

secretGenerator:
  - name: redis-secret
    namespace: redis
    files:
      - values.yaml=secret/values.yaml