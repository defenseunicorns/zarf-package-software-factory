---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: gitlab-redis-secret
  namespace: bigbang
spec:
  targetNamespace: redis
  releaseName: new-gitlab-redis-secret
  interval: 2m
  timeout: 2m
  chart:
    spec:
      chart: charts/raw
      sourceRef:
        kind: GitRepository
        name: zarf-package-software-factory
        namespace: flux-system
  test:
    enable: false
  install:
    remediation:
      retries: -1
  upgrade:
    remediation:
      retries: 5
      remediateLastFailure: true
    cleanupOnFail: true
  rollback:
    timeout: 10m
    cleanupOnFail: false
  # dependsOn:
  #   - name: bigbang
  #     namespace: bigbang
  dependsOn:
    - name: redis
      namespace: softwarefactoryaddons
  valuesFrom:
    - kind: Secret
      name: new-redis-secret
      valuesKey: values.yaml
---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: gitlab-redis-secret-2
  namespace: bigbang
spec:
  targetNamespace: gitlab
  releaseName: new-gitlab-redis-secret
  interval: 2m
  timeout: 2m
  chart:
    spec:
      chart: charts/raw
      sourceRef:
        kind: GitRepository
        name: zarf-package-software-factory
        namespace: flux-system
  test:
    enable: false
  install:
    remediation:
      retries: -1
  upgrade:
    remediation:
      retries: 5
      remediateLastFailure: true
    cleanupOnFail: true
  rollback:
    timeout: 10m
    cleanupOnFail: false
  dependsOn:
    - name: bigbang
      namespace: bigbang
  valuesFrom:
    - kind: Secret
      name: new-redis-secret
      valuesKey: values.yaml